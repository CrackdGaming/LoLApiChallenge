@model LoLApiChallenge.Models.FactionDetailsModel

<h3>@Model.Name</h3>
@foreach (var champ in Model.Champs)
{
    <div class="faction-stats-wrap col-lg-8">
        <img src="@champ.ImageUrl" />
        <div>@champ.Name</div>
    </div>
}
