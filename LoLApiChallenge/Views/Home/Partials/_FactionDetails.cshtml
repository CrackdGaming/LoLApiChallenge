@model LoLApiChallenge.Models.FactionDetailsModel

<h3>@Model.Name</h3>
@foreach (var champ in Model.Champs)
{
    <div class="faction-stats-wrap col-lg-4">
        <img src="@champ.ImageUrl" width="100" height="100" />
        <div>@champ.Name</div>
    </div>
}
